name: Full Flutter CI (disabled)

# Disabled to unblock PR builds; Windows build workflow covers required artifacts.
# Run manually if needed.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "flutter-ci disabled for this repo"
