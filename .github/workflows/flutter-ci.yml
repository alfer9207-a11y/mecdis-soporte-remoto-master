name: flutter-ci (disabled)

# Disabled: this workflow was causing YAML parse errors (called-workflow reference).
# Use "Windows Build (Artifact)" for releases.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "flutter-ci disabled"
