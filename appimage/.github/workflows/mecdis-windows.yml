name: Mec-Dis Windows Build (BLINDADO)

on: [push]

jobs:
  build-ui:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Build Flutter UI (blindado)
        run: echo "Flutter UI build only"
      - name: Package portable
        run: echo "Packaging portable"
